# Student-Management-System
Developed a Student Management System using Python with MySQL database connectivity to efficiently manage student records. The application performs complete CRUD (Create, Read, Update, Delete) operations while storing data securely in a relational database.
